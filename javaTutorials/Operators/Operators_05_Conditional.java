package Operators;

public class Operators_05_Conditional {

    static void main() {
        int age = 28;

        String isValid = (age >= 18) ? "Valid" : "Invalid";

        System.out.println("The boolean value of the variable 'isValid' is : " + isValid);
    }

}
